import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin(() => {
  // Plugin can be used for client-side initialization if needed in the future
  // Currently, all functionality is handled via composables and server utilities
})
